name: adventure_card_game
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.4 <3.0.0'


# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  test_coverage: ^0.4.2
# meta: 1.6.0             # solve problem on sdk 2.14.4 and test_coverage: ^0.4.2. ...
                          # Error: The property 'displayString' is defined in multiple ...
                          # ... extensions for 'TargetKind' and neither is more specific.
